require 'pry-byebug'

def stock_picker(prices)
    if prices.is_a?
    # find the max price in the array
    # find the smallest price in the array that has a smaller index number than the largest.
    # return an array containing the index values of those two numbers.
   else
    return 'Type Error. Please provide an array as an argument.'
   end
end